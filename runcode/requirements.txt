# Core Python dependencies for the deployable DRAFTS search runtime.
numpy
astropy
matplotlib
seaborn
tqdm
opencv-python
torch
torchvision

# Install a CuPy build that matches the node's CUDA toolkit separately, for
# example: python -m pip install cupy-cuda12x
#
# The optional Numba detector benchmark also requires:
# python -m pip install numba
