# Common CPU-side dependencies for data generation, model training,
# inference, and search orchestration.
numpy
pandas
scipy
scikit-image
scikit-learn
h5py
astropy
matplotlib
seaborn
tqdm
opencv-python
timm
albumentations
ultralytics
torch
torchvision

# GPU search entry points also require a CuPy package matching the installed
# CUDA toolkit (for example, cupy-cuda12x). Install that package separately.
# The optional Numba benchmark backend additionally requires numba.
